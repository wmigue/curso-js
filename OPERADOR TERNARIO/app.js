"strict mode";

//OPERADOR TERNARIO: REEMPLAZAMOS EL IF, CONSUME MENOS RECURSOS.

let edad = 15;

(edad > 18) ?
    (
        console.log("es mayor")
    )

    :
    
    (
        console.log("es menor"),
        console.log("no puede pasar.")
    )